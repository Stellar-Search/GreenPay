{
  "name": "@greenpay/stellar-client",
  "version": "1.0.0",
  "description": "Shared Stellar SDK client factory for GreenPay (Horizon, Soroban/RPC, network passphrase resolution)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "@stellar/stellar-sdk": "^12.0.0"
  },
  "devDependencies": {
    "@stellar/stellar-sdk": "^12.0.0",
    "typescript": "^5.3.3",
    "vitest": "^3.2.7"
  },
  "license": "UNLICENSED",
  "private": true
}
